# Лабораторная работа 4. Построение конвейера обработки даннных

🎯 **Цель работы:**

Освоить принципы построения модульных приложений с разделением ответственности между компонентами.
Получить навыки работы с коллекцией ArrayList

📋 **Задачи**

1. Реализовать ядро обработки данных (DataProcessor) с обработкой числовых данных по индивидуальному варианту

[Индивидуальные варианты](course-materials/labs/tasks/dataprocessing.java)

2. Разработать универсальный конвейер обработки, работающий с массивом строк как универсальным форматом данных

[Теоритическая справка](course-materials/topik/dataprocessing.md)

3. Освоить взаимные преобразования между разными представлениями данных:

 - Массив строк ↔ Коллекция ArrayList<String>

 - Строковое представление ↔ Числовые данные (Double)

 - Коллекции ↔ Массивы

4. Создать два клиентских приложения с одним ядром обработки данных (DataProcessor)

- Консольное приложение (работа с файлами)

- Графическое приложение JavaFX (работа с текстовым вводом)


📋 **Ожидаемые результаты**

1. Модуль core, включающий DataProcessor с методом processPipeline(String[]) и FileUtils для работы с файлами

2. Консольное приложение ConsoleApp с функциями:

- Чтение аргументов командной строки имя файла данных, имя файла результата

- Загрузка данных из файла

- Использование DataProcessor.processPipeline()

- Вывод результатов в файл и\или консоль

3. Графическое приложение WindowApp (JavaFX), включающее

- Простой интерфейс: TextArea (входные данные) + Button + ListView(выходные)

- Использование того же processPipeline()

- Демонстрация работы с текстовым вводом

*Цель не просто написать код для обработки чисел, а понять и продемонстрировать архитектурный подход, когда правильно спроектированная система позволяет легко создавать разные интерфейсы поверх одной бизнес-логики.*

# Порядок выполнения

*Шаг 1* Подготовка проекта

- Создать структуру папок согласно схеме выше
- Подготовить тестовые данные, как тектовый файл минимум 10 числами (по одному на строку)
- Создать пустые классы-заглушки во всех пакетах

```text
Lab4_DataProcessing/
├── src/
│   ├── core/                    // Ядро системы (не зависит от UI)
│   │   ├── DataProcessor.java   //Конвейер обработки данных
│   │   └── FileUtils.java       // Утилиты работы с файлами
│   ├── console/                 // Консольное приложение
│   │   └── ConsoleApp.java      // Точка входа для консоли
│   ├── gui/                     // Графическое приложение
│   │   └── WindowApp.java       // Точка входа для JavaFX
│   └── models/                  // Модели данных
│       └── InputArgs.java       // Параметры командной строки
├── test/                        // Тестовые данные
│   ├── input01.txt
│   ├── input02.txt
│   └── ... 
└── output/                      // Результаты обработки
```


**Шаг 2** Реализация метод обработки 

1. Класс DataProcessor с реализацией метода processData(List<Double> input) по варианту
Пример: отфильтровать положительные числа и возвести их в квадрат

2. Реализовать метод *processPipeline(String[])* по алгоритму
1) Преобразовать строки в числа (с обработкой ошибок)
2) Вызвать processData()
3) Преобразовать результаты обратно в строки
4) Добавить вспомогательные методы для преобразований

З. Класс FileUtils для чтения и записи чисел из/в файла в List<Double>


**Шаг 3** Консольное приложение 

1. Класс InputArgs, реализующий разбор аргументов командной строки:

*java -jar app.jar -i input.txt [-o output.txt]*

2. Класс *ConsoleApp*, в котором использовать *InputArgs* для получения параметров, FileUtils - для загрузки данных,  DataProcessor.processPipeline() для обработки
Вывести результаты в консоль и (опционально) в файл

**Шаг 4** Графическое приложение 

1. Класс WindowApp для создание простого JavaFX-приложение без FXML

Интерфейс: TextArea (ввод) + Button + ListView (вывод)

При нажатии кнопки Получить текст из TextArea, Разделить на строки
Вызвать DataProcessor.processPipeline() (тот же метод!)
Отобразить результаты в ListView

**Шаг 5** Тестирование и анализ 

Протестировать оба приложения на одинаковых данных

Убедиться, что результаты идентичны

Проанализировать преимущества модульного подхода